<UserControl
    x:Class="VisualThreading.ToolWindows.BuildingWindowControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid
        ShowGridLines="True"
        VerticalAlignment="Stretch"
        HorizontalAlignment="Stretch"
        MinWidth="200"
        MinHeight="200">

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <WebBrowser
            Grid.Row="0"
            Grid.Column="0"
            Name="Browser"
            Width="Auto"
            Height="Auto"
            LoadCompleted="WebBrowser_LoadCompleted" />

        <StackPanel
                Grid.Row="1"
                Background="{DynamicResource {x:Static SystemColors.InfoBrushKey}}"
                Orientation="Horizontal" Name="Controls"
                MinHeight="20">

            <Button

                    Name="ShowCodeButton"
                    Click="ShowCodeButton_Click"
                    Content="Show Code From C#" Margin="5,5,5,5" HorizontalAlignment="Right" VerticalAlignment="Center" />
            <Button
                    Name="InsertCode"
                    Content="Insert code" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5,5,5,5" />
        </StackPanel>
    </Grid>
</UserControl>